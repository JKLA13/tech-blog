 {{#if user.posts.length}}
 <section>
<div>
    {{user.username}}
    <h3>Blog Post History</h3>
    {{#each user.posts as |post|}}
    <div>
    {{> post-details }}
    </div>
    {{/each}}
</div>
{{/if}}

</section>
<div>
    <button class="w-100 btn btn-md btn-primary m-1" id="new-blog-button">Add Tech Post</button>
</div>

<script src="/js/dashboard.js"></script>