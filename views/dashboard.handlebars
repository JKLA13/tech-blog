{{#if posts.length}}
<section>
    <h3>Blog Post History</h3>
    {{#each posts as |post|}}
    </div>
    {{> dashboard post-info post}}
    {{/each}}
</div>
{{/if}}

<div>
    <button class="w-100 btn btn-lg btn-primary m-1"
    onclick="location.href='/new-blog-post'">+ Add Tech Post</button>
</div>

{{!-- <script src="/js/#"></script> --}}